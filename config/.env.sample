# NewsNewt Environment Configuration
# Copy this file to .env and customize as needed

# ============================================
# CRAWLER CONFIGURATION
# ============================================

# Maximum number of concurrent scraping operations
# Lower values (1-2) = Conservative, lower resource usage
# Medium values (3-5) = Balanced for typical workloads
# Higher values (6+) = Aggressive, requires more CPU/memory
# Default: 3
CRAWL_CONCURRENCY=3

# ============================================
# LOGGING CONFIGURATION
# ============================================

# Logging verbosity level
# Options: DEBUG, INFO, WARNING, ERROR
# - DEBUG: Detailed diagnostic information (use for development/troubleshooting)
# - INFO: General informational messages (recommended for production)
# - WARNING: Warning messages for potential issues
# - ERROR: Error messages only
# Default: INFO
LOG_LEVEL=INFO

# ============================================
# PLAYWRIGHT CONFIGURATION
# ============================================

# Run browser in headless mode (no visible UI)
# Set to 'false' for debugging/development to see the browser
# Set to 'true' for production (required for Docker environments)
# Default: true
PLAYWRIGHT_HEADLESS=true

# ============================================
# STEALTH MODE
# ============================================

# Enable stealth mode to avoid CAPTCHA detection
# Stealth mode modifies browser fingerprint and hides automation indicators
# Set to 'true' to reduce CAPTCHA trigger rate (recommended)
# Set to 'false' for debugging or sites without bot detection
# Default: true
ENABLE_STEALTH=true

# ============================================
# TIMEZONE (Optional)
# ============================================

# Timezone for log timestamps and scheduling
# Use standard timezone format (e.g., America/New_York, Europe/London)
# Default: UTC
TZ=UTC

# ============================================
# USAGE NOTES
# ============================================

# For Docker Deployment:
# - These values are set in compose.yml
# - You can override them by creating a .env file in the project root
# - Docker Compose will automatically load .env and use these values

# For Local Development:
# - Copy this file to .env in the project root
# - Customize values as needed
# - The application will load .env automatically via python-dotenv (if added)
# - Make sure .env is in .gitignore to avoid committing secrets

# ============================================
# ADDITIONAL CONFIGURATION
# ============================================

# If you need to add API keys or other sensitive configuration,
# add them here and ensure .env is in .gitignore:
# API_KEY=your-secret-key-here

